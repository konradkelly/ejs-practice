<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>EJS Practice</title>
	</head>
	<body>
		<h1>EJS Practice</h1>
        <%- include('partials/header') %>
		<h2>Hello, <%= person.name %></h2>

		<h3>Occupation: <%= person.oc %></h3>

		<% if (person.occupation === "plumber") { %>
		<p>You work with pipes!</p>
		<% } else { %>
		<p>That's an interesting job!</p>
		<% } %> <% if (person.fc === "red") { %>
		<p>Favorite Color: <%= person.fc %>. You are cool!</p>
		<% } else { %>
		<p>Favorite Color: <%= person.fc %>. You are not cool!</p>
		<% } %>

        <%- include('partials/footer') %>
	</body>
</html>
